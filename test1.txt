(if (> k 5)
    (setq k 10)
    (print k)
)
(setq d (+ g 4))
(setq a (+ g 4))
(setq s (+ g 4))
(setq d (+ g 4))
(setq f (+ g 4))
(setq g (+ g 4))
(setq h (+ g 4))
(setq j (+ g 4))
(setq k (+ g 4))
(setq l (+ g 4))
(setq q (+ g 4))
(setq w (+ g 4))
(setq r (+ g 4))
(setq t (+ g 4))
(setq y (+ g 4))
(setq u (+ g 4))
(if (not (> d k))
    (setq d 10)
    (print d)
)
