(setq a 5)
(setq b 5)
(setq c (+ a 3))
(setq b (+ c 3))
(setq d (+ b 3))
(setq y 45)
(setq x 4)
(loop while (< x 3)
    (if (< 2 y)
        #adkfjhfsjdkj
        (if (< y 34)
            (setq x 45)
            (setq x 34)
        )
        (print F)
    )
    (setq my-str "PARTH SHAH")
    (print my-str)
)